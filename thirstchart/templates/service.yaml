apiVersion: v1
kind: Service
metadata:
  name: {{ include "thirstchart.fullname" . }}
  labels:
    {{- include "thirstchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thirstchart.selectorLabels" . | nindent 4 }}
