apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thirstchart.fullname" . }}-test-connection"
  labels:
    {{- include "thirstchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thirstchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
